node_modules/
/dist/
build/
*/build/
.gradle/
docs/
.superpowers/
# The TeaVM bundle is build output; the .d.ts emitted beside it from the same Java IS committed, so
# the TypeScript seam type-checks on a clean clone before anything is built.
src/generated/custom-provider.teavm.js
.dynamic-providers.json
.idea/

# Generated on the default branch by the release readme job.
README.md
