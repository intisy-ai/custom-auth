// Endpoint resolution + handleIr composition. Zero-dependency beyond :ir (TeaVM-clean:
// no java.net/nio/reflection/threads), so :teavm-custom can transpile this module verbatim.
dependencies {
    compileOnly project(":ir")
    testImplementation project(":ir")

}
